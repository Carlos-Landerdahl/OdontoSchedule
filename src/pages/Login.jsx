import LoginForm from "../Components/Login/Index";

export const Login = () => {
    return (
        <main id="wrapperLogin">
          <section>
            <h1>Login</h1>
            <LoginForm/>
          </section>
        </main>
    );
};

export default Login;
